class Animal {
    constructor(name) {
        this.name = name
    }

    walk() {
        `${this.name} walking`
    }

    eat() {
        `${this.name} eating`
    }

    sleep() {
        `${this.name} sleeping`
    }
}
